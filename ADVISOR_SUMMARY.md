# 阶段性进展汇报：评测框架干扰的识别与消除

**日期**: 2026-09-16
**Git Tag**: v3-interference-fix-60pct
**Benchmark**: FAB (Finance Agent Benchmark) 公开 50 题

---

## 核心发现

评测框架本身（工具设计、调用格式、超参数、评分机制）可以是最大的干扰源。我们在 FAB 50 题上的初步实验显示，DeepSeek-V4-Flash 的准确率从表面上的 **34%**，在排除框架干扰后恢复至 **60%**（5 题验证集）——**26 个百分点的"假性能力不足"**由评测框架导致，而非模型本身能力问题。

其中：
- **纯代码 Bug**（3 个）：修复后恢复 +6pp（34%→40%），属工程修复，无学术价值
- **设计层干扰**（5 个已确认）：修复后恢复 +20pp（40%→60%），是核心研究问题

---

## Bug vs 干扰的区分

| 类别 | 定义 | 示例 | 学术价值 |
|---|---|---|---|
| Bug | 代码有错误，修复即消除 | Schema 标记遗漏、缓存代码位置错 | 无（工程修复） |
| 干扰 | 代码正确，但设计/参数/机制系统性偏移公平性 | 工具设计要求 LLM 传 2000 字符文本 | **核心研究贡献** |

---

## 已确认的干扰（5 个，有因果量化数据）

### 1. 工具设计与调用范式不兼容（+20pp，影响最大）

**现象**: retrieve_information 工具 105 次调用全部失败（0%），导致 24/27 个 complete_failure

**为什么是干扰而非 Bug**:
- 代码完全正确：函数接收 text 参数、处理文本、返回匹配句
- 但设计要求 LLM 在 JSON 参数中传递 2000+ 字符的 SEC 文档文本
- LLM 在技术上无法在 JSON 中精确复制大段文本
- 即使代码无任何 bug，工具仍然不可用

**修复**: 改为有状态设计——parse_html 自动缓存输出，retrieve_information 只接收 query 参数并搜索缓存

**因果量化**: RI 成功率 0%→95%，准确率 40%→60%（+20pp）

### 2. 调用格式的"格式税"（+52pp，V4-Pro 案例）

**现象**: 同一模型在 ReAct 文本格式下 8.3%，在原生 Function Calling 下 60.39%（FAB 官方数据）

**为什么是干扰**: 两种实现的代码都正确，但 ReAct 需要文本解析（正则匹配 TOOL:/ARGS:），解析失败=工具调用失败，这系统性地惩罚了不擅长格式遵从的模型

### 3. 负面示例的 Priming Effect（+18pp，避免回归）

**现象**: Fallback prompt 中列出具体禁止短语后，LLM 模仿了这些被禁止的模式，准确率从 34% 回归到 16%

**为什么是干扰**: Prompt 代码完全正确（按设计工作），但包含负面示例这个设计选择本身导致 LLM 行为偏移

### 4. Fallback Prompt 截断（+4pp）

**现象**: "ONE LINE ONLY" 限制导致答案被截断，丢失关键信息

**为什么是干扰**: 代码正确执行了截断逻辑，但这个超参数选择系统性截断了信息

### 5. API 失败归因（定性影响）

**现象**: 瞬时基础设施失败（超时、限流、credits 耗尽）被计为 LLM 能力不足

**为什么是干扰**: 代码正确执行了评分逻辑，但未区分"模型答错"和"API 连不上"

**修复**: 3 次重试 + api_failure 标注 + 双准确率报告（含/排除 API 失败）

---

## 提出的框架

### 公平评估三原则

- **P1 不干扰**: 评测框架不得补偿 LLM 的工具调用缺陷（如自动填充缺失参数）
- **P2 不引导**: 工具示例必须用抽象占位符，不能用具体内容（避免 priming effect）
- **P3 不归因**: 瞬时基础设施失败不得计入 LLM 能力评分

### 干扰注册表

系统追踪所有潜在干扰（INT-01~14），标注：
- **类型**: 工具设计 / 格式 / Prompt / 超参数 / 机制 / 评分
- **状态**: 已确认（有因果数据）/ 疑似（需实验）/ 非干扰
- **因果影响**: 前后准确率对比（pp）

### Bug vs 干扰识别流程

```
现象 → 排除 Bug（确认代码正确）→ 分析设计/参数/机制 → 量化因果影响 → 记入注册表
```

---

## 证据体系

| 层级 | 内容 | 位置 |
|---|---|---|
| Git 版本快照 | v0-baseline-34pct, v1-native-fc, v3-interference-fix-60pct | git tags |
| 实验日志 | trajectories, results, error reports, logs | experiments/ |
| 干扰因果表 | 每个干扰的前后对比 + delta | INTERFERENCE_CAUSAL_TABLE.md |
| 评测标准 | 方法论、原则、注册表 | EVALUATION_STANDARD.md v2.3 |

---

## 下一步

1. **全量验证**: FAB 50 题跑修复后版本，验证 60% 是否可复现
2. **疑似干扰验证**: INT-05(max_steps)、INT-06(context)、INT-07(judge 自评) 的控制实验
3. **Benchmark 扩展**: 框架稳定后接入 FinGAIA / BigFinanceBench 做跨 benchmark 一致性
4. **论文方向**: "评测框架干扰的识别、归因与消除"作为核心贡献

---

## 一句话总结

> 评测框架中一个工具的设计缺陷（要求 LLM 传大段文本作为 JSON 参数），在代码完全正确的情况下，将模型真实能力从 60% 压低至 34%。这类"代码正确但设计不公允"的干扰无法通过修 Bug 发现，需要对工具设计与调用范式的兼容性进行系统性分析。我们提出干扰注册表和公平评估三原则作为方法论贡献。
